
<!-- views/index.ejs -->
<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="utf-8" />

    <title>Fitness App</title>
    <meta name="description" content="The HTML5 Herald" />
    <meta name="author" content="SitePoint" />

    <link rel="stylesheet" type="text/css" href="/css/web.css" />
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Serif&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@9"></script>
    <script language="JavaScript"  src="http://ajax.googleapis.com/ajax/libs/jquery/1.10.0/jquery.min.js"></script>
    <script defer> 
    
    var message = <%- JSON.stringify(message) %>;    
 
   
    window.addEventListener('load', (event) => {
      
      if(message!="none"){
       Swal.fire({icon:"info",
       text:message
      })
    }
    /*
      if(logins){
        if(logins!="Successfully Logged In"){
       Swal.fire({icon:"error",
       text:logins
      })
    }
    }
      if(signup){
      Swal.fire({icon:"info",
       text:signup
      })
    }
    */
}); 
      
    </script>
   
  </head>

  <body>
    <div class="container" >
      <li><a href="/">Home</a></li>
      <li> <a id="calories" href="/calories">Calories Calculator</a></li>
      <li><a id= "history" href ="/history">My History</a></li>
    
     </div>


    <form action="/login" method ="POST" >
      <div class="container" id="login">
        
        <i class="material-icons md-48 ">account_circle</i>
        <label>Email</label>
        <input
          type="email"
          name="email"
         
          onFocus="field_focus(this,'email');"
          onblur="field_blur(this, 'email');"
          class="email"
        />
        <label>Password</label>
        <input
          type="password"
          name="password"
          height="20"
          onFocus="field_focus(this, 'password');"
          onblur="field_blur(this, 'password');"
          class="password"
        />

        
        
        <button type="submit" class="signupbtn" id="btn1">Sign In</button>
       
        <a href="/signup" id="signup"><button type="button">Sign up</button></a>
       
        
      </div>
    </form>


   
  </body>
</html>
